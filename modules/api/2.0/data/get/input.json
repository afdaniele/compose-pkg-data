{
    "type": "object",
    "description": "input",
    "properties": {
        "database": {
            "type": "string",
            "description": "Name of the database the given key belongs to"
        },
        "key": {
            "type": "string",
            "description": "Key of the entry to retrieve"
        },
        "seek": {
            "type": "string",
            "description": "Path to the region of the record to fetch (requires a JSON record). Ex. /objects/cars/tesla/model_s"
        }
    },
    "required": [
        "database",
        "key"
    ]
}
