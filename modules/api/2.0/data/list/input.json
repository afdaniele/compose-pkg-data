{
    "type": "object",
    "description": "input",
    "properties": {
        "database": {
            "type": "string",
            "description": "Name of the database to list the keys for"
        }
    },
    "required": [
        "database"
    ]
}
